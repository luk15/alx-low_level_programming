#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @i: returns an int
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		i = i;
	}
	else
	{
		i = i * -1;
	}
	return (i);
}
